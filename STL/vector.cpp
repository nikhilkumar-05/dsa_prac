#include<bits/stdc++.h>
using namespace std;

void explainvector(){
    vector<int> v;
    v.push_back(1);
    v.emplace_back(2);

    vector <pair<int , int>> v1;
    v1.push_back({1,2});
    v1.emplace_back(2,2);

    vector<int> v3(5,20) ;
    vector<int> v4(v3);
    for(auto:i){
        cout<< x <<;
    };

}
int main(){
    explainvector();
}